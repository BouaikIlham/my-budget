<header>
  <div>
    <h1>Opertions
      <h1>
      </div>
      <% @operations.each do |opertion|%>
        <div class=" border-solid border-2 border-cyan-900 flex justify-around space-x-10 mx-10 m-20">
          <h1><%= opertion.name%></h1>
          <p><%= opertion.amount%></p>
          <%= button_to "Delete", opertion, method: :delete%>
        </div>
      <% end%>
    </div>
  </header>
